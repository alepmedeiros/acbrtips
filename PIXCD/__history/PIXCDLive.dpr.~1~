program PIXCDLive;

uses
  System.StartUpCopy,
  FMX.Forms,
  principal in 'principal.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
