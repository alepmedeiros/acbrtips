unit boleto.repository.impl.sacado;

interface

uses
  boleto.repository.interfaces,
  boleto.component.interfaces,
  ACBrBoleto;

type
  TSacado = class(TInterfacedObject, ISacado)
  private
    FParent: ITitulo;
    FTitulo: TACBrTitulo;

    constructor Create(Parent: ITitulo);
  public
    class function New(Parent: ITitulo): ISacado;

    function NomeSacado(Value: String): ISacado;
    function CNPJCPF(value: String): ISacado;
    function Logradouro(Value: string): ISacado;
    function Numero(Value: String): ISacado;
    function Bairro(Value: string): ISacado;
    function Cidade(value: String): ISacado;
    function UF(Value: String): ISacado;
    function CEP(Value: String): ISacado;
    function &End: ITitulo;
  end;

implementation

function TSacado.Bairro(Value: string): ISacado;
begin
  Result := Self;
  FParent.This
end;

function TSacado.CEP(Value: String): ISacado;
begin

end;

function TSacado.Cidade(value: String): ISacado;
begin

end;

function TSacado.CNPJCPF(value: String): ISacado;
begin

end;

constructor TSacado.Create(Parent: ITitulo);
begin
  FParent := Parent;
  FTitulo := TACBrTitulo(FParent.This);
end;

function TSacado.&End: ITitulo;
begin
  Result := FParent;
end;

function TSacado.Logradouro(Value: string): ISacado;
begin

end;

class function TSacado.New(Parent: ITitulo): ISacado;
begin
  Result := Self.Create(Parent);
end;

function TSacado.NomeSacado(Value: String): ISacado;
begin

end;

function TSacado.Numero(Value: String): ISacado;
begin

end;

function TSacado.UF(Value: String): ISacado;
begin

end;

end.
